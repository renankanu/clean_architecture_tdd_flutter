name: clean_architecture_tdd_flutter
description: Study project using SOLID, Clean Architecture, TDD and Design Patterns.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  test:
  mockito: ^5.0.7
  faker: ^2.0.0-rc.2
  build_runner: ^2.0.1

flutter:
  uses-material-design: true